#include <iostream>
using namespace std;
int tambah(int x, int w, int y, int z) {
    hasil = x + w + y + z;
    return hasil;
}
int main() {
    int x, w, y, z;
    int operasi;
    cout << "" switch (operasi){
        case 1 :

    } re
}